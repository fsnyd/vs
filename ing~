#include <iostream>

using namespace std;
int arr[100];
int cnt = 0;
void func(int depth,int n)
{
	if (depth == n)
	{
		for (int i = 0; i < n; i++)
		{
			/*for (int j = 0; j < n - 1; j++)
			{
				if (arr[j] == arr[j + 1])
				{
					cnt++;
					break;
				}
				if (arr[0] == arr[n-1])
				{
					cnt++;
					break;
				}
			}
			if (cnt ==0)*/
			cout << arr[i]<<" ";
		}
	/*	if (cnt == 0)*/
		cout << endl;
		cnt = 0;
		return;
	}
//	system("pause");
	for (int i = 1; i < 7; i++)
	{
		cnt = 0;
		for (int j = 0; j < depth; j++)
		{
			if (arr[j] == i)
			{
				cnt = 1;
				break;
			}
		}
		if (cnt == 1)
			continue;
		arr[depth] = i;
		func(depth+1,n);
	}
}
int main()
{
	int n;
	int m;
	cin >> n;

	func(0,n);
	return 0;
}
