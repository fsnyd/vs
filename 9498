#include <iostream>
#include <string>
using namespace std;
int main(void)
{
	int n;
	char res = 'F';
	cin >> n;

	if (n >= 60)
		res = 'D';
	if(n>=70)
		res = 'C';
	if (n >= 80)
		res = 'B';
	if (n >= 90)
		res = 'A';

	cout << res;
	return 0;
}
